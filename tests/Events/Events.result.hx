package com.example;

import flash.display.MovieClip;
import flash.events.Event;

class Events extends MovieClip
{		
	@:meta(Event(name="complete",type="Event"))

	@:meta(Event(name="open",type="Event"))

	@:meta(Event(name="close",type="Event"))
}