package com.example;

import flash.display.MovieClip;

class Casts extends MovieClip
{
	public function new()
	{
		Std.int(10.1);
		
		(Std.parseInt("100"));
		
		Std.parseFloat("10");
		
		Std.string(100);
		
		var sprite:Sprite = cast(this, Sprite);
		
		var sprite:Sprite = cast(this, Sprite);
		
		if (Std.is(this,MovieClip))
		{
		}
	}
}